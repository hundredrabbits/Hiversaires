Flexible aspect ratio
    Rethink Layout
        Probably center on screen
    Those SVGs
    Walk around, see if any state changes are jarring

Once you show IOO⠛, fold their new stuff into the build

Switch JS files to ES6 modules once Electron supports them
    Already supported by expo.io, supposedly
