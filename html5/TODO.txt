Derive symbol names from comments in seal door code and git log
    Then use them to give more context to other data and code

Isolate puzzle state from nodes
    Node image changes should be renamed to the original node image, plus some modifier
    Templates should go somewhere else

Break the dozenal code into sensible chunks

Convert the world to a displayable graph, if you can
Full walkthrough

There must be a way to make git DUMP EVERY VERSION OF EVERY FILE

Bug someone about using import/export es6 syntax

NOTES
    templateKillUpdate works, but nothing causes its condition to be true

The illusion image swap only applies to one location
    Ask Devine about it
